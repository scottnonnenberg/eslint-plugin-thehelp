'use strict';

// eslint-disable-next-line no_mutation
module.exports = {
  rules: {
    'absolute_or_current_dir': 'error',
    'no_mutation': 'error',
    'no_array_mutation': 'error',
  },

  reportUnusedDisableDirectives: true,
};
